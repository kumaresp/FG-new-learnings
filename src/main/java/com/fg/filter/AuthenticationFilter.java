package com.fg.filter;

public class AuthenticationFilter{ 
	
}/*
	 * extends OncePerRequestFilter {
	 * 
	 * 
	 * @Autowired private HandlerExceptionResolver resolver;
	 * 
	 * @Autowired private AuthenticationManager authenticationManager;
	 * 
	 * @Override protected void doFilterInternal(HttpServletRequest request,
	 * HttpServletResponse response, FilterChain filterChain) throws
	 * ServletException, IOException {
	 * 
	 * try { buildSecurityContext(request); filterChain.doFilter(request, response);
	 * } catch (Exception ex) { resolver.resolveException(request, response, null,
	 * ex); }
	 * 
	 * }
	 * 
	 * private void buildSecurityContext(HttpServletRequest request) {
	 * 
	 * // UsersAuthenticationDetails usersAuthenticationDetails=new
	 * UsersAuthenticationDetails(true);
	 * 
	 * }
	 * 
	 * }
	 */